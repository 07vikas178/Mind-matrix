<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ data-extraction rules. Empty = use default.
  Required because the manifest references it on targetApi 31+.
-->
<data-extraction-rules>
    <cloud-backup />
    <device-transfer />
</data-extraction-rules>
